#include <stdio.h>
/**
 * main - (a program that prints a line with printf)
 * return: (0) (Outcome means the program is successful)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
